# iOSDevDirectory Jekyll Site

To properly set up for deployment, after cloning this repository execute the following:

```
git clone -b gh-pages --single-branch git@github.com:daveverwer/iOSDevDirectory-Web.git _site
```
